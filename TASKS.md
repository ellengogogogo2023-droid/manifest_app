# MVP 任务拆解（TASKS）

> 当前目标：只验证核心链路，不做数据库、登录认证、存储与发布能力。
> 核心链路：用户填写表单 -> 生成冥想文本 -> 转为音频 -> 播放。

---

## 执行规则（Copilot 必读）

- Phase 1 已完成。
- Phase 2 按编号顺序执行：一次只做一个任务，完成后停下等待我确认，再进入下一个。
- 未经我确认，不允许跳过任务或合并多个任务一起做。
- Phase 3 全部视为延期任务，在当前实现中忽略，不创建相关文件、不改动相关配置。

---

## Phase 1（当前 MVP 范围）

### P1-01 - 项目初始化（Expo + TypeScript）

- [x] 目标
  - 搭建 React Native + Expo 基础项目，能在 iOS/Android/Web 启动。
- 涉及文件
  - package.json、tsconfig.json、app.json、babel.config.js、app/_layout.tsx
- 验收标准
  - npx expo start 能启动
  - TypeScript 无阻塞报错

### P1-02 - 基础 UI 骨架（输入页 -> 播放页）

- [x] 目标
  - 建立两页导航：app/index.tsx（输入页）、app/player.tsx（播放页）
  - 输入页可跳转到播放页并传递必要参数
- 涉及文件
  - app/_layout.tsx、app/index.tsx、app/player.tsx
- 验收标准
  - 两个页面可渲染
  - 输入页测试按钮可跳转播放页
  - iOS/Android 无明显布局溢出

### P1-03 - 输入表单页面

- [x] 目标
  - 在输入页实现四项输入：Goal、Scene、Feeling、Duration（5/10/15）
  - Goal 和 Scene 必填
- 涉及文件
  - src/components/meditation/GoalInputForm.tsx
  - src/types/meditation.types.ts
  - app/index.tsx
- 验收标准
  - 四项可交互
  - 未填必填项时禁用提交或显示错误提示

### P1-03A - 引导页与视觉升级

- [x] 目标
  - 在表单页之前新增一个吸引力法则（Law of Attraction）说明页
  - 全局页面视觉改为莫兰蒂色系
  - 表单页新增语言选择（English / Chinese）
- 涉及文件
  - app/index.tsx
  - app/form.tsx
  - app/_layout.tsx
  - app/player.tsx
  - src/components/meditation/GoalInputForm.tsx
  - src/types/meditation.types.ts
- 验收标准
  - 首次进入 App 先看到说明页，点击后进入表单页
  - 页面配色为统一莫兰蒂风格
  - 提交表单时包含 language 字段

### P1-04 - 后端 API（生成冥想文本，Claude）

- [x] 目标
  - 提供一个可调用的 API（本地 Node 服务或 Serverless 均可）
  - 接收 goal/scene/feeling/durationMinutes
  - 调用 Claude API，基于既定 Prompt 返回结构化冥想文本
- 涉及文件（建议）
  - backend/src/routes/generate-meditation.ts
  - backend/src/prompts/meditationPrompt.ts
  - src/types/api.types.ts
- 验收标准
  - POST 请求可返回 meditationId 与 scriptText
  - scriptText 包含 Opening / Body / Closing 结构

### P1-04A - 文本预览/检查页（生成音频前）

- [x] 目标
  - 在“生成文本”完成后，不自动进入 TTS，先进入文本检查页
  - 展示完整冥想文本，并可视化高亮结构标记（如 `[SECTION:...]`、`[PAUSE:Xs]`）
  - 提供“重新生成”和“确认，生成音频”两个按钮
  - 允许在检查页临时调整输入并重新生成文本（无需返回表单页）
- 涉及文件（建议）
  - app/script-review.tsx
  - src/hooks/useMeditationGenerate.ts
  - src/stores/meditationStore.ts
  - app/form.tsx
- 验收标准
  - 生成文本后不会自动开始生成音频，而是停在文本检查页
  - 检查页能清楚看到完整文本和结构标记
  - 点击“确认，生成音频”后才继续原有 TTS + 播放流程
  - 点击“重新生成”可重新调用文本生成接口并更新展示内容

### P1-05 - 后端 API（TTS 转音频）

- [x] 目标
  - 提供一个可调用的 TTS API
  - 输入 scriptText，输出可播放的音频地址（先固定一种声音）
- 涉及文件（建议）
  - backend/src/routes/generate-audio.ts
  - src/types/api.types.ts
- 验收标准
  - POST 请求可返回 audioUrl
  - 前端可用该 audioUrl 播放

### P1-06 - 前端调用与播放逻辑

- [x] 目标
  - 提交表单后串联流程：生成文本 -> 文本检查 -> 生成音频
  - 展示生成进度（至少两段文案）
  - 成功后跳转播放页并可播放音频
  - 实现基础播放能力：play/pause/进度条
- 涉及文件
  - src/services/meditation.service.ts
  - src/hooks/useMeditationGenerate.ts
  - src/hooks/useAudioPlayer.ts
  - src/components/common/Button.tsx
  - src/components/common/LoadingOverlay.tsx
  - src/components/common/ErrorMessage.tsx
  - src/components/meditation/AudioPlayer.tsx
  - src/stores/meditationStore.ts
  - src/utils/formatDuration.ts
  - app/index.tsx、app/player.tsx
- 验收标准
  - 可以走通完整链路并听到音频
  - loading、错误提示、播放控制可用

---

## Phase 2: 参与感与打卡功能

> 执行流程：严格按 P2-01 → P2-24 的编号顺序，一次只做一个任务，完成并确认后再进入下一个。
> 任务顺序按需求优先级排列，不按技术依赖重排；确有前置依赖的在描述中注明。

### 需求 1：Daily Check-in（每日打卡）

#### P2-01 - 打卡数据模型与本地持久化

- [ ] 目标
  - 定义打卡条目的数据结构（日期、是否完成冥想、情绪评分、意图文本、创建时间）
  - 建立本地持久化读写能力（按日期存取、覆盖当日、读取全部）
- 涉及模块
  - 类型定义模块、状态管理模块、本地存储服务模块
- 验收标准
  - 可以写入一条当日打卡记录并在重启 App 后读取到
  - 按日期查询返回唯一记录，不存在时返回空

#### P2-02 - 每日打卡页面与入口

- [ ] 目标
  - 新增打卡页面路由，并在首页提供明显入口
  - 页面包含「今天是否完成冥想」的是/否选择
  - 提交按钮调用 P2-01 的存储能力写入记录
- 涉及模块
  - 路由/页面模块、首页模块、打卡组件模块、状态管理模块
- 验收标准
  - 从首页可进入打卡页
  - 选择是/否并提交后，记录被正确保存
  - 未做选择时提交按钮禁用或给出提示

#### P2-03 - 情绪评分（1-5 分）组件

- [ ] 目标
  - 实现 1-5 分情绪评分选择组件（含视觉反馈），接入打卡表单
- 涉及模块
  - 打卡组件模块、主题/样式模块、类型定义模块
- 验收标准
  - 可选中 1-5 中任一分值并有明确选中态
  - 提交后评分随打卡记录一起保存
  - 未选评分时给出必填提示（或按设计允许留空并明确记录为空）

#### P2-04 - 「今天的小意图」可选输入

- [ ] 目标
  - 在打卡表单增加一行可选文字输入（一句话意图），限制合理字数
- 涉及模块
  - 打卡组件模块、类型定义模块
- 验收标准
  - 留空可正常提交
  - 填写后内容随打卡记录保存并可读取
  - 超出字数上限时有提示或自动截断

#### P2-05 - 每日一次限制与当日状态回显

- [ ] 目标
  - 同一天只允许一条有效打卡记录
  - 当日已打卡时，页面展示已打卡状态并回显已填内容，支持修改覆盖
- 涉及模块
  - 状态管理模块、本地存储服务模块、打卡页面模块、日期工具模块
- 验收标准
  - 当日重复进入打卡页时显示「今日已打卡」并回显数据
  - 修改后保存不会新增第二条当日记录
  - 跨天后可正常创建新一天的记录

#### P2-06 - 打卡完成后跳转当日 Journal 页

- [ ] 目标
  - 打卡提交成功后自动跳转到当日 Journal 页面（本任务先跳转到占位页，页面内容由需求 2 实现）
- 涉及模块
  - 路由/页面模块、打卡页面模块
- 验收标准
  - 提交成功后自动进入当日 Journal 路由，并携带正确日期参数
  - 返回操作不会回到已提交的打卡表单造成重复提交

### 需求 2：Journal 文字部分（依赖需求 1 的打卡与日期机制）

#### P2-07 - Journal 数据模型与按天归档存储

- [ ] 目标
  - 定义日记条目数据结构（日期、引导问题、正文、创建/更新时间）
  - 实现按天归档的本地读写（按日期读取、写入、列出全部日期）
  - 依赖：复用 P2-01/P2-05 的日期与存储约定
- 涉及模块
  - 类型定义模块、本地存储服务模块、状态管理模块
- 验收标准
  - 同一天写入多次只更新同一条目
  - 可按日期倒序列出全部已有日记日期

#### P2-08 - 每日引导问题机制

- [ ] 目标
  - 建立引导问题文案库，并实现「每天固定展示一个问题」的选取规则（同一天多次进入结果一致）
- 涉及模块
  - 常量/文案模块、工具函数模块、Journal 页面模块
- 验收标准
  - 同一天多次打开显示同一个问题
  - 不同日期能取到不同问题
  - 问题文案随日记条目一并保存，历史回溯时展示当天的问题

#### P2-09 - Journal 文字记录页

- [ ] 目标
  - 实现当日 Journal 页面：展示引导问题 + 多行文本输入 + 保存
  - 已有当日内容时自动回显并可继续编辑
- 涉及模块
  - 路由/页面模块、Journal 组件模块、状态管理模块、主题/样式模块
- 验收标准
  - 输入文字并保存后，重新进入页面能看到已保存内容
  - 空内容不产生无效条目（或按设计明确处理）
  - 键盘弹出时输入区域不被遮挡

#### P2-10 - Journal 历史列表与详情回溯

- [ ] 目标
  - 新增历史页：按日期倒序列出所有日记条目（含摘要与打卡情绪信息）
  - 点击某天可进入该天详情查看完整内容
- 涉及模块
  - 路由/页面模块、Journal 组件模块、本地存储服务模块
- 验收标准
  - 列表按日期倒序展示，条目数与实际存储一致
  - 点击任一天可查看当天的引导问题与完整正文
  - 无数据时显示空状态提示

### 需求 3：冥想文本可编辑与多版本保存

#### P2-11 - 文本预览页支持编辑

- [ ] 目标
  - 在现有文本预览/检查页增加编辑态，允许直接修改生成的冥想文本
  - 编辑后的文本用于后续 TTS 生成
- 涉及模块
  - 文本预览页面模块、状态管理模块
- 验收标准
  - 可进入编辑态修改文本并保存回状态
  - 保存后的文本在「确认，生成音频」时被真实使用
  - 取消编辑可还原为修改前内容

#### P2-12 - 冥想文本多版本存储

- [ ] 目标
  - 定义版本数据结构（版本号、文本、来源：AI 生成/用户编辑、时间戳）
  - 每次生成或保存编辑时追加一个版本并持久化
- 涉及模块
  - 类型定义模块、状态管理模块、本地存储服务模块
- 验收标准
  - 多次编辑保存后能读取到多条版本记录，顺序与时间一致
  - 重启 App 后版本列表仍存在

#### P2-13 - 版本历史查看与切换

- [ ] 目标
  - 在文本预览页提供版本列表入口，可查看历史版本内容并切换为当前使用版本
- 涉及模块
  - 文本预览页面模块、通用组件模块、状态管理模块
- 验收标准
  - 版本列表展示时间与来源，可预览任一版本全文
  - 切换版本后预览页内容更新，且生成音频使用被切换的版本

### 需求 4：中文 TTS 与男/女声切换

#### P2-14 - 中文语音接入与后端 voice 参数支持

- [ ] 目标
  - 先确认并记录当前 TTS 供应商可用的高质量中文语音（至少一男一女）
  - 后端音频生成接口新增 voice 参数，按参数选择对应语音，未传时保持现有默认行为
- 涉及模块
  - 后端音频生成路由模块、后端配置模块、API 类型定义模块
- 验收标准
  - 传入不同 voice 参数可生成音色明显不同的中文音频
  - 不传 voice 时行为与改动前一致
  - 非法 voice 值返回明确错误信息

#### P2-15 - 前端男声/女声切换

- [ ] 目标
  - 在表单页或文本预览页提供男声/女声选择，并将 voice 参数传递给后端
  - 记住用户上次选择
- 涉及模块
  - 表单/预览页面模块、前端服务模块、状态管理模块、类型定义模块
- 验收标准
  - 选择男声/女声后生成的音频音色与选择一致
  - 再次进入 App 时默认沿用上次选择

### 需求 5：Journal 图片部分（依赖需求 2 的文字日记）

#### P2-16 - 相册/相机权限请求流程

- [ ] 目标
  - 接入相册与相机权限申请流程：首次使用时请求、被拒绝时给出说明与跳转系统设置的引导
  - 补充 iOS/Android 所需的权限声明配置文案
- 涉及模块
  - App 配置模块、权限工具/Hook 模块、通用提示组件模块
- 验收标准
  - 首次触发添加图片时弹出系统权限申请
  - 拒绝权限后有可读的解释提示与前往设置的引导，App 不崩溃
  - iOS/Android 权限描述文案已配置

#### P2-17 - 图片选取与存储方案落地

- [ ] 目标
  - 支持从相册选择或拍照获取图片
  - 先落地本地存储方案：图片保存到 App 私有目录，日记条目仅保存相对路径引用；对图片做压缩/尺寸限制
  - 在实现前明确记录本地存储与后续云端（如对象存储）迁移的取舍与预留接口
- 涉及模块
  - 类型定义模块、本地存储/文件服务模块、Journal 状态管理模块
- 验收标准
  - 可从相册与相机两种来源添加图片并成功落地到本地目录
  - 重启 App 后图片仍可正常加载
  - 存储路径为相对引用，不硬编码绝对路径
  - 单张图片体积经过压缩控制在设定上限内

#### P2-18 - 日记图片展示、预览与删除

- [ ] 目标
  - 在当日 Journal 页与历史详情页展示图片缩略图，支持点击放大预览与删除
  - 删除时同步清理本地文件
- 涉及模块
  - Journal 组件模块、路由/页面模块、本地存储/文件服务模块
- 验收标准
  - 缩略图正常显示，点击可全屏预览
  - 删除后条目与本地文件均被清理，无残留占用
  - 历史条目中的图片可正常回溯查看

### 需求 6：背景音乐叠加

#### P2-19 - 背景音乐资源与选择 UI

- [ ] 目标
  - 准备 3-5 种轻音乐/频率音（含 Solfeggio 类）资源与版权说明
  - 提供背景音乐选择界面（含「无背景音乐」选项）与试听
- 涉及模块
  - 资源/常量模块、播放页或预览页模块、通用组件模块
- 验收标准
  - 可看到 3-5 个可选项并成功试听
  - 选择结果被保存到状态中
  - 默认可选择「无背景音乐」且不影响原流程

#### P2-20 - 背景音乐与冥想音频叠加播放

- [ ] 目标
  - 实现冥想人声与所选背景音乐的同时播放，背景音乐循环并支持独立音量控制
  - 播放/暂停/结束时两路音频状态保持同步
- 涉及模块
  - 音频播放 Hook 模块、播放器组件模块、状态管理模块
- 验收标准
  - 播放时可同时听到人声与背景音乐，背景音量明显低于人声且可调
  - 暂停/继续/结束时两路音频同步，无残留播放
  - 冥想音频结束后背景音乐自动淡出或停止

### 需求 7：音频播放增强（技术复杂度较高）

#### P2-21 - 音频能力调研与方案确认（前置任务）

- [ ] 目标
  - 确认当前 Expo SDK 版本与所用音频库（expo-av / expo-audio 等）对离线缓存、后台播放、锁屏控制的原生支持程度
  - 输出结论：是否需要更换音频库、是否需要 development build 或 config plugin、以及各子任务的实现路径
- 涉及模块
  - 依赖配置模块、App 配置模块、音频播放 Hook 模块、技术文档
- 验收标准
  - 形成明确的书面结论与选型建议，覆盖 iOS 与 Android 两端
  - 若需更换库或改用 development build，给出迁移影响范围评估
  - 后续 P2-22 ~ P2-24 的实现方案基于本结论确定

#### P2-22 - 离线播放（音频本地缓存）（技术复杂度较高）

- [ ] 目标
  - 生成的音频下载缓存到本地，播放优先读取缓存
  - 提供缓存索引与清理策略（容量或数量上限）
- 涉及模块
  - 文件/缓存服务模块、音频播放 Hook 模块、状态管理模块
- 验收标准
  - 首次播放后断网仍可完整播放该音频
  - 缓存命中时不再发起网络请求
  - 缓存超出上限时按策略清理且不影响正在播放的音频

#### P2-23 - 后台播放（技术复杂度较高）

- [ ] 目标
  - 配置 iOS/Android 后台音频能力，App 切到后台或息屏时音频不中断
  - 处理来电、其他 App 抢占音频焦点后的恢复逻辑
- 涉及模块
  - App 配置模块、音频播放 Hook 模块、依赖配置模块
- 验收标准
  - 切到后台或息屏后音频继续播放
  - 被打断后恢复行为符合预期，不出现静默卡死
  - iOS 与 Android 均验证通过

#### P2-24 - 锁屏控制（技术复杂度较高）

- [ ] 目标
  - 在锁屏/通知中心展示当前冥想信息，并支持播放/暂停等基础控制
  - 锁屏控制与 App 内播放状态双向同步
- 涉及模块
  - 音频播放 Hook 模块、App 配置模块、播放器组件模块
- 验收标准
  - 锁屏界面显示标题与播放控制，点击可控制播放/暂停
  - 锁屏操作后回到 App，界面播放状态与实际一致
  - iOS 与 Android 均验证通过

---

## Phase 3（延期，不在当前范围内）

### P3-A - Supabase 与数据库

- [ ] 初始化 Supabase 项目
- [ ] 建立 meditations 表与 migration
- [ ] RLS 策略（匿名读写策略后续收紧）
- [ ] 使用 Supabase Storage 保存音频
- [ ] 将后端 API 切换为 Supabase Edge Functions

### P3-B - 认证与账号体系

- [ ] 用户登录/注册
- [ ] 会话管理
- [ ] 按用户隔离历史数据

### P3-C - 数据持久化与历史记录

- [ ] 保存冥想历史
- [ ] 历史列表与详情页
- [ ] 错误重试与幂等策略

### P3-D - 发布与工程化增强

- [ ] 生产环境配置与密钥管理
- [ ] 监控、日志、告警
- [ ] 自动化测试与 CI/CD

---

## 任务总览

| 编号 | 任务 | 阶段 | 状态 |
|---|---|---|---|
| P1-01 | 项目初始化（Expo + TypeScript） | Phase 1 | ✅ |
| P1-02 | 基础 UI 骨架（输入页 -> 播放页） | Phase 1 | ⬜ |
| P1-03 | 输入表单页面 | Phase 1 | ⬜ |
| P1-03A | 引导页与视觉升级 | Phase 1 | ⬜ |
| P1-04 | 后端 API（Claude 生成冥想文本） | Phase 1 | ⬜ |
| P1-04A | 文本预览/检查页（生成音频前） | Phase 1 | ⬜ |
| P1-05 | 后端 API（TTS 转音频） | Phase 1 | ⬜ |
| P1-06 | 前端调用与播放逻辑 | Phase 1 | ⬜ |
| P2-01 | 打卡数据模型与本地持久化 | Phase 2 | ⬜ |
| P2-02 | 每日打卡页面与入口 | Phase 2 | ⬜ |
| P2-03 | 情绪评分（1-5 分）组件 | Phase 2 | ⬜ |
| P2-04 | 「今天的小意图」可选输入 | Phase 2 | ⬜ |
| P2-05 | 每日一次限制与当日状态回显 | Phase 2 | ⬜ |
| P2-06 | 打卡完成后跳转当日 Journal 页 | Phase 2 | ⬜ |
| P2-07 | Journal 数据模型与按天归档存储 | Phase 2 | ⬜ |
| P2-08 | 每日引导问题机制 | Phase 2 | ⬜ |
| P2-09 | Journal 文字记录页 | Phase 2 | ⬜ |
| P2-10 | Journal 历史列表与详情回溯 | Phase 2 | ⬜ |
| P2-11 | 文本预览页支持编辑 | Phase 2 | ⬜ |
| P2-12 | 冥想文本多版本存储 | Phase 2 | ⬜ |
| P2-13 | 版本历史查看与切换 | Phase 2 | ⬜ |
| P2-14 | 中文语音接入与后端 voice 参数支持 | Phase 2 | ⬜ |
| P2-15 | 前端男声/女声切换 | Phase 2 | ⬜ |
| P2-16 | 相册/相机权限请求流程 | Phase 2 | ⬜ |
| P2-17 | 图片选取与存储方案落地 | Phase 2 | ⬜ |
| P2-18 | 日记图片展示、预览与删除 | Phase 2 | ⬜ |
| P2-19 | 背景音乐资源与选择 UI | Phase 2 | ⬜ |
| P2-20 | 背景音乐与冥想音频叠加播放 | Phase 2 | ⬜ |
| P2-21 | 音频能力调研与方案确认（技术复杂度较高） | Phase 2 | ⬜ |
| P2-22 | 离线播放（音频本地缓存）（技术复杂度较高） | Phase 2 | ⬜ |
| P2-23 | 后台播放（技术复杂度较高） | Phase 2 | ⬜ |
| P2-24 | 锁屏控制（技术复杂度较高） | Phase 2 | ⬜ |
| P3-A | Supabase 与数据库 | Phase 3 | ⏸ |
| P3-B | 认证与账号体系 | Phase 3 | ⏸ |
| P3-C | 数据持久化与历史记录 | Phase 3 | ⏸ |
| P3-D | 发布与工程化增强 | Phase 3 | ⏸ |
